<form method="post" action="{{ path('app_token_delete', {'id': token.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ token.id) }}">
    <button class="btn">Delete</button>
</form>
